# ======================================================================
# Configuration file for git_sync.sh
# Set environment-specific variables here.
# Variables defined here can be overridden by explicit environment exports
# or command-line arguments (e.g., --remote-name=my_staging).
# ======================================================================

# --- Core Repository Details ---

# The full URL for cloning (REQUIRED for init-and-sync)
GIT_REPO_URL="https://github.com/your-username/your-repo.git"

# The Git remote alias (e.g., origin or upstream)
GIT_REMOTE_NAME="origin"

# The branch on the remote to target (e.g., main or develop)
GIT_REMOTE_BRANCH="main"


# --- Optional Local Configuration ---

# The specific local branch to operate on. If left empty, the currently
# checked-out branch will be used.
# GIT_LOCAL_BRANCH="feature-branch"

# The local directory name where the repo should be cloned. If empty,
# Git uses the repo name from the URL.
# GIT_LOCAL_DIR="my-project-checkout"
